import React from 'react';
import Main from '../../../Components/Main/main';

export default function ComboCaculator(): JSX.Element {
    return <Main title='Combo Damage Caculator' content={<div />} />;
}

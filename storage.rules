service firebase.storage {
  match /b/{bucket}/o {
    match /Dice%20Images {
       match /{allImages=**} {
         allow read;
         allow write: if request.auth.token.isEditor == true;
       }
    }
  }
}
